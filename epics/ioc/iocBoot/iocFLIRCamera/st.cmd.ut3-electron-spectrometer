dbLoadDatabase("$(TOP)/dbd/spinnakerApp.dbd")
spinnakerApp_registerRecordDeviceDriver(pdbbase) 

epicsEnvSet("GENICAM_DB_FILE", "$(ADGENICAM)/db/FLIR_BFS_31S4M.template")

# Prefix for all records
epicsEnvSet("PREFIX", "E:")

# Really large queue so we can stream to disk at full camera speed
epicsEnvSet("QSIZE",  "200")   
# The maximim image width; used for row profiles in the NDPluginStats plugin
epicsEnvSet("XSIZE",  "2048")
# The maximim image height; used for column profiles in the NDPluginStats plugin
epicsEnvSet("YSIZE",  "1536")
# The maximum number of time series points in the NDPluginStats plugin
epicsEnvSet("NCHANS", "2048")
# The maximum number of frames buffered in the NDPluginCircularBuff plugin
epicsEnvSet("CBUFFS", "500")
# The search path for database files
# This is for Linux
epicsEnvSet("EPICS_DB_INCLUDE_PATH", "$(ADCORE)/db:$(ADGENICAM)/db:$(ADSPINNAKER)/db")
# Define NELEMENTS to be enough for a 2048x2048x3 (color) image
epicsEnvSet("NELEMENTS", "3145728")

####################################################################################################################
# The port name for the detector
epicsEnvSet("PORT",   "Spectrometer:Pointing")
ADSpinnakerConfig("$(PORT)", "23243498")
asynSetTraceIOMask($(PORT), 0, 2)

# Main database.  This just loads and modifies ADBase.template
dbLoadRecords("$(ADSPINNAKER)/db/spinnaker.template", "P=$(PREFIX),R=$(PORT):,PORT=$(PORT)")

# Load the autogenerated file of GenICam features
dbLoadRecords("$(GENICAM_DB_FILE)", "P=$(PREFIX),R=$(PORT):,PORT=$(PORT)")

# NDPva
NDPvaConfigure("$(PORT):PVA", $(QSIZE), 0, "$(PORT)", 0, $(PREFIX)Pva:$(PORT):Image, 0, 0, 0)
dbLoadRecords("NDPva.template",  "P=$(PREFIX),R=Pva:$(PORT):, PORT=$(PORT):PVA,ADDR=0,TIMEOUT=1,NDARRAY_PORT=$(PORT)")

# Pointing image statistics
NDStatsConfigure("$(PORT):STATS", $(QSIZE), 0, "$(PORT)", 0, 0, 0, 0, 0, $(MAX_THREADS=5))
dbLoadRecords("NDStats.template",  "P=$(PREFIX),R=Stats:$(PORT):,  PORT=$(PORT):STATS, ADDR=0,TIMEOUT=1,HIST_SIZE=256,XSIZE=$(XSIZE),YSIZE=$(YSIZE),NCHANS=$(NCHANS),NDARRAY_PORT=$(PORT)")
NDTimeSeriesConfigure("$(PORT):STATS_TS", $(QSIZE), 0, "$(PORT):STATS", 1, 1, 0, 0, 0, 0)
dbLoadRecords("$(ADCORE)/db/NDTimeSeries.template", "P=$(PREFIX),R=Stats:TS:$(PORT):, PORT=$(PORT):STATS_TS,ADDR=0,TIMEOUT=1,NDARRAY_PORT=$(PORT):STATS,NDARRAY_ADDR=1,NCHANS=$(NCHANS),ENABLED=0")

# Add Tiff file save
NDFileTIFFConfigure("$(PORT):TIFF", $(QSIZE), 0, "$(PORT)", 0, 0)
dbLoadRecords("$(ADCORE)/db/NDFileTIFF.template",  "P=$(PREFIX),R=Tiff:$(PORT):,PORT=$(PORT):TIFF,ADDR=0,TIMEOUT=1,NDARRAY_PORT=$(PORT)")
dbpf("$(PREFIX)Tiff:$(PORT):FilePath", "/tmp/ndfiletiff-test")
dbpf("$(PREFIX)Tiff:$(PORT):EnableCallbacks", "Enable")

####################################################################################################################
# The port name for the detector
epicsEnvSet("PORT",   "Spectrometer:LowEnergy")
ADSpinnakerConfig("$(PORT)", "20541743")
asynSetTraceIOMask($(PORT), 0, 2)

# Main database.  This just loads and modifies ADBase.template
dbLoadRecords("$(ADSPINNAKER)/db/spinnaker.template", "P=$(PREFIX),R=$(PORT):,PORT=$(PORT)")
# Load the autogenerated file of GenICam features
dbLoadRecords("$(GENICAM_DB_FILE)", "P=$(PREFIX),R=$(PORT):,PORT=$(PORT)")

# NDPva
NDPvaConfigure("$(PORT):PVA", $(QSIZE), 0, "$(PORT)", 0, $(PREFIX)Pva:$(PORT):Image, 0, 0, 0)
dbLoadRecords("NDPva.template",  "P=$(PREFIX),R=Pva:$(PORT):, PORT=$(PORT):PVA,ADDR=0,TIMEOUT=1,NDARRAY_PORT=$(PORT)")

####################################################################################################################
# The port name for the detector
epicsEnvSet("PORT",   "Spectrometer:HighEnergy")
ADSpinnakerConfig("$(PORT)", "20125390")
asynSetTraceIOMask($(PORT), 0, 2)

# Main database.  This just loads and modifies ADBase.template
dbLoadRecords("$(ADSPINNAKER)/db/spinnaker.template", "P=$(PREFIX),R=$(PORT):,PORT=$(PORT)")

# Load the autogenerated file of GenICam features
dbLoadRecords("$(GENICAM_DB_FILE)", "P=$(PREFIX),R=$(PORT):,PORT=$(PORT)")

# NDPva
NDPvaConfigure("$(PORT):PVA", $(QSIZE), 0, "$(PORT)", 0, $(PREFIX)Pva:$(PORT):Image, 0, 0, 0)
dbLoadRecords("NDPva.template",  "P=$(PREFIX),R=Pva:$(PORT):, PORT=$(PORT):PVA,ADDR=0,TIMEOUT=1,NDARRAY_PORT=$(PORT)")

####################################################################################################################
set_requestfile_path("$(ADGENICAM)/GenICamApp/Db")
set_requestfile_path("$(ADSPINNAKER)/spinnakerApp/Db")


####################################################################################################################
# TakeNShots database
dbLoadRecords("$(TOP)/TakeNShotsApp/Db/TakeNShots.db")
set_requestfile_path("$(TOP)/TakeNShotsApp/Db/TakeNShots.db");

