# Use this compose file for local testing

services:
  image_analysis_complete_handler:
    build: .
    environment:
      IMAGE_BACKEND_REDIS_HOST: redis
      IMAGE_BACKEND_REDIS_PORT: 6379
      IMAGE_BACKEND_REDIS_USERNAME: johndoe
      IMAGE_BACKEND_REDIS_PASSWORD: ""
  redis:
    image: redis:latest
    ports:
      - 6379:6379
